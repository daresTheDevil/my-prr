{
  "name": "prrip-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Pearl River Resort Integration Platform Monorepo",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "turbo run format",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "pnpm": "^9.0.0",
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}